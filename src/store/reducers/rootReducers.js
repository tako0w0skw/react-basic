const initState = {
	users: [
		{ id: "01", name: "Hue Nhu" },
		{ id: "02", name: "Trong Thang" },
	],
};

const rootReducer = (state = initState, action) => {
	return state;
};

export default rootReducer;
